/**
 * Constants mostly refer to standard units, hash-codes and other fixed values.
 * Although there is no strict rule to keep ALL constants here,
 * If you use the same SCREAMING_CASE variable in multiple places, I recommend you to put it here.
 */
export {};
